# in case latex generator is down
sudo apt update
sudo apt install texlive-latex-base texlive-latex-recommended texlive-latex-extra texlive-fonts-recommended texlive-extra-utils latexmk perl xzdec
sudo apt-get update && sudo apt-get install -y texlive-bibtex-extra

# metric for unlearning algorithm running instructions

drag CASIA-SURF CeFA: A Benchmark for Multi-modal Cross-ethnicity Face Anti-spoofing 
    label .txt file and sample folder into 23comp_metric/data (loader reads all images of directory title of label <person-id> under the /color directory)

run ./run.sh, the metric should be running
/checkpoint saves base model
/outputs saves retrain baseline models (all variations)

observe terminal logs for progress